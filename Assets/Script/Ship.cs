using UnityEngine;
using System.Collections;
using System;

public class Ship : MonoBehaviour {
	/*public GameObject Breakbulk;
	Vector3 startPosition = Vector3.zero;
	Vector3 endPosition = Vector3.up * 10f;
	float timer;
	// Use this for initialization


	void Start () {
		Instantiate (Breakbulk);

		float step = speed * Time.deltaTime;
		target.positio 
		transform.position = Vector3.MoveTowards (transform.position, target.position, step);
		Example ();

}
	
	// Update is called once per frame
	void Update () {

		timer += Time.deltaTime;
		transform.position = Vector3.Lerp(startPosition, endPosition, timer / 20f);
		move ();
	
	}

	public GameObject breakbulk;

	void Example() {
		int i = 0;
			while (i < 10){
		  	GameObject ship = Instantiate(breakbulk, new Vector3(i * 2.0F, i * 2.0f, 0), Quaternion.Euler(new Vector3(0,0,270))) as GameObject;
			ship.name = "ship_" + i.ToString();
			i++;
		}
	}

	void move(){
		GameObject .Find ("ship_1").transform .Translate (0, 0.1f, 0);
	}


*/


}
